if [ -z "${iso_path}" ]; then
    set iso_path="/path/to/trafktux.iso"
fi

probe -u $root --set=imgdevpath
export imgdevpath

set img_dev="/dev/disk/by-uuid/${imgdevpath}"
export img_dev
export iso_path

loopback loop "${iso_path}"
set root=loop

source /grub/grub.cfg
