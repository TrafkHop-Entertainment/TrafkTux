---
backend: pacman
skip_if_no_internet: false
update_db: true
operations:
  - install: []

# Die tatsaechlichen Pakete kommen zur Laufzeit aus der packagechooser-Auswahl
# (method: packages dort). Unbedingt einmal mit "calamares -d" testen und
# das Install-Log pruefen, ob die gewaehlten Pakete wirklich ankommen -
# das Zusammenspiel packagechooser<->packages kann je nach Calamares-Version
# minimal variieren.
