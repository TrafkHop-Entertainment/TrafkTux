# Notwendig damit Suspend/Resume mit nvidia-open-dkms funktioniert.
# Ohne das bleibt der Bildschirm nach dem Aufwachen oft schwarz oder
# das System haengt beim Resume.
options nvidia NVreg_PreserveVideoMemoryAllocations=1
options nvidia NVreg_TemporaryFilePath=/var/tmp
